// 跨域的另一种方式 是 w3c的另一种跨域请求的方式
window.onload = function() {
    console.log( 'haha' );
    // 由于标准浏览器 和 IE 浏览器创建含有 差别 我们 需要进行功能检测的问题
    // var xhr = new XMLHttpRequest();
    var xhr = null;
    if( xhr ){

    }

}